package com.aura.main.model.transfer

/**
 * Transfer content screen state
 *
 * @param fieldIsOK all the screen text fields are completed.
 * @param result the transfer result.
 */
data class TransferContent(val fieldIsOK: Boolean, val result: Boolean)