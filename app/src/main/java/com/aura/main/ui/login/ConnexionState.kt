package com.aura.main.ui.login

/**
 * Enumeration of all different available state of the login screen.
 */
enum class ConnexionState {
    INITIAL,
    CHAMPS_REMPLIS,
    ERREUR_CONNEXION,
    CONNEXION_EN_COURS,
    CONNEXION_REUSSIE
}