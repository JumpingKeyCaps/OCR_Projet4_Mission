package com.aura.main.ui.transfer

/**
 * Enumeration of all different available state of the transfer screen.
 */
enum class TransferState {
    IDLE,
    FIELDS_OK,
    LOADING,
    ERROR,
    FAIL,
    SUCCESS
}